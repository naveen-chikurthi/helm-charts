apiVersion: v1
data:
  default-realm.json: |
    {
        "id":"pib-realm",
        "realm":"pib-realm",
        "enabled":true
    }
kind: ConfigMap
metadata:
  name: keycloak-realm-configmap
  