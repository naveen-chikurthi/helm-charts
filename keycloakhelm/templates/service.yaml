apiVersion: v1
kind: Service
metadata:
  name: {{ include "keycloakhelm.fullname" . }}
  labels:
    {{- include "keycloakhelm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "keycloakhelm.selectorLabels" . | nindent 4 }}
